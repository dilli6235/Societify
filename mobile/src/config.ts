/**
 * API base URL the app talks to.
 *
 * ⚠️ Expo Go on a physical phone CANNOT reach `localhost` — it must be a
 * publicly reachable URL (the Cloudflare tunnel) or your machine's LAN IP.
 *
 * Update this whenever the tunnel URL changes, or point it at your deployed
 * API once that's live.
 */
export const API_URL = 'https://drag-missed-humanities-gains.trycloudflare.com/api/v1';
